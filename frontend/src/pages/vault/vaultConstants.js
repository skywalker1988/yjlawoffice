/**
 * vaultConstants — 문서 보관소 페이지에서 사용하는 상수 정의
 */

/** 한 페이지에 표시할 문서 수 */
export const PAGE_LIMIT = 20;

/** 검색 디바운스 지연 시간(ms) */
export const SEARCH_DEBOUNCE_MS = 500;

/** 중요도 필터 옵션 (높은 순) */
export const IMPORTANCE_OPTIONS = [5, 4, 3, 2, 1];

/** 업로드 허용 파일 확장자 */
export const ALLOWED_UPLOAD_EXTENSIONS = ["md", "markdown", "txt"];
